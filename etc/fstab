# /etc/fstab
# -------------------------------------------
# Specify filesystems for automatic mounting.
# -------------------------------------------
# Using uuids to specify the filesystem sources
# ensures they are found correctly even if devices
# are added, changed, or removed. The shell command
# `blkid` lists all uuids that are currently visible.

# -------------------------------------------------------------------
# | fs.source | m.target | fs.type | m.options | fs.dump | fs.check |
# -------------------------------------------------------------------

# The automatic mounting of efi, root, and swap is controlled
# by linux parameters and therefore not specified in this file.
